{
  "name": "@sovereignbase/convergent-replicated-list-wasm",
  "private": true,
  "type": "module",
  "scripts": {
    "configure": "emcmake cmake --preset wasm-release",
    "build": "cmake --build --preset wasm-release && cmake --install build/wasm-release --prefix dist",
    "smoke": "node test"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "dependencies": {
    "uuid": "^14.0.0"
  }
}
