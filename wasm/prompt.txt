Refactor the convergence engine so Rust/Wasm owns the CRDT convergence state, while JavaScript owns all actual JS values, consumer interaction, and network interaction.

Hard rule:

JS<->WASM MUST only exchange:

- `number`
- `bigint`
- `externref`

No JSON serialization.

No MsgPack serialization at the JS/Wasm boundary.

No serde.

No object copying.

No JS object materialization inside Wasm.

JavaScript receives snapshots and deltas from the consumer/network. Rust/Wasm is given references to those snapshot/delta/value objects as `externref`, plus the required `id` and `previousId` values as BigInts once.

Wasm converts the BigInt IDs into its own internal representation and keeps the convergence/indexing state. JS does not need to keep BigInt IDs for normal traversal or lookup.

Wasm does the hard part:

- merge ordering
- predecessor resolution
- tombstone handling
- shuffled delta application
- visible index mapping
- convergence state
- deterministic materialization order

JavaScript does the simple part:

- owns the actual values
- owns consumer API calls
- owns network I/O
- asks Wasm for references by numeric index
- returns those references to the consumer/network

Start the lazily rewriting utilizing C:\Users\jorts\convergent-replicated-list\.skills\optimize-performance\SKILL.md at every step to verify the benefit.

Ensure test pass and that we win absolutely all benchmarks.

Both externref and number JS<->WASM passes are like a couple of nanoseconds, BigInt is like around 10 nanoseconds but it only happens once and, we already win most of the JS benchmarks so as we expect WASM (RUST) to be way more performant than JS we can just do the exact current typescript implementation of convergence logic but in rust and OPTIMIZED FOR RUST SPECIFICALLY.

So basically semantically the exact same convergence implementation, but for more perfomant runtime, optimized for that runtime.

And basically the idea is that the rust makes "instructions" the JS can complete in O(1) or O(n) based on the convergence rust knows/resolves fastest.

Keep variable, function method, prop names and all semantics and documentation etc style.